# Router stubs — each module will be filled in subsequent steps.
# Importing from here keeps main.py clean.
from app.routers import (
    auth, users, departments, academic_years,
    proposals, comments, documents, similarity,
    allocations, repository, notifications,
    reports, admin, projects, deadlines, config, health,
)
